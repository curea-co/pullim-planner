PORT=4030
# Phase β: DB 미연결로 common 인프라 스모크 라우트만 검증 (기본값 false).
# Phase γ entity 도입 시 DATABASE_ENABLED=true 로 전환하고 아래 DB env 활성화.
# DATABASE_ENABLED=false
# DATABASE_URL=postgres://pullim:pullim_local@localhost:5432/pullim_planner
