{
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    "rootDir": "src",
    // fetch / URL / URLSearchParams 등 WHATWG 전역을 쓰는 브라우저용 fetch 래퍼라
    // DOM lib 을 명시한다 (base 의 ES2022 만으로는 미정의).
    "lib": ["ES2022", "DOM"]
  },
  "include": ["src/**/*"]
}
