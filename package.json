{
  "name": "pullim-planner-monorepo",
  "version": "0.1.0",
  "private": true,
  "workspaces": [
    "apps/*",
    "packages/*"
  ],
  "scripts": {
    "dev": "turbo dev",
    "dev:planner": "bun --filter @pullim-planner/planner dev",
    "dev:backend": "bun --filter @pullim-planner/backend dev",
    "build": "turbo build",
    "build:planner": "bun --filter @pullim-planner/planner build",
    "build:backend": "bun --filter @pullim-planner/backend build",
    "lint": "turbo lint",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "db:up": "docker compose up -d",
    "db:down": "docker compose down",
    "db:reset": "docker compose down -v && docker compose up -d"
  },
  "devDependencies": {
    "turbo": "^2.7.4",
    "typescript": "^5.7.3"
  },
  "packageManager": "bun@1.3.12"
}
