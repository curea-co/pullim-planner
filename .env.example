# pullim-api(통합 IdP + planner 도메인) base — 세션(쿠키 SSO + CSRF)·planner 데이터 모두 여기로.
#   · 로그인/세션: auth-context → pullimSession (lib/auth/pullim-session-client.ts)
#   · planner 데이터(/planner/*): lib/planner/client → pullim-client
# prefix 없음. 미설정 시 http://localhost:3000.
#   local(SSO): http://api.pullim.local:3000   (FE 는 planner.pullim.local:3006 — same-site 쿠키)
#   dev:        https://dev-api.pullim.ai      (FE 는 dev-planner.pullim.ai)
#   prod:       https://api.pullim.ai          (FE 는 planner.pullim.ai)
# ⚠️ localhost 로 두면 *.pullim.local 접속 시 cross-site 라 로그인 후 /planner/me 가 계속 비인증 —
#    LOGIN_URL(아래)과 같은 등록도메인으로 함께 맞출 것.
NEXT_PUBLIC_PULLIM_API_URL=http://api.pullim.local:3000
# non-HttpOnly CSRF 쿠키 이름(env별). local=local-pullim-csrf / dev=dev-pullim-csrf / prod=prod-pullim-csrf.
NEXT_PUBLIC_PULLIM_CSRF_COOKIE=local-pullim-csrf

# 중앙 로그인 base — 미인증 시 ${LOGIN}/login?next= 로 위임(SSO). 게이트키퍼 확정: pullim.ai/login(apex).
# ⚠️ 환경별 필수(코드 기본값 없음 — 미설정 시 명시적 실패). 호스트는 *.pullim.local/apex(localhost 금지·쿠키 공유).
#   local: http://pullim.local:3001  /  dev: 배포 env  /  prod: https://pullim.ai
NEXT_PUBLIC_PULLIM_LOGIN_URL=http://pullim.local:3001

# 공통 헤더(서비스 전환) 비-플래너 서비스 호스트(OS). local: http://os.pullim.local:3001 / prod: https://os.pullim.ai
NEXT_PUBLIC_PULLIM_OS_URL=http://os.pullim.local:3001

# 로컬 dev 전용 — 인증 게이트 우회(mock 렌더). next dev + localhost 계열 호스트에서만 동작(3중 가드).
# 실 SSO 검증 시 0. 절차: proc/2026-06-29_planner-local-sso-setup.md
NEXT_PUBLIC_DEV_AUTH_BYPASS=1

# 개발자 리셋 버튼(로그아웃+시드 복원) 노출 스위치. `next dev`(NODE_ENV!==production)에서는 이 값과
# 무관하게 항상 노출된다 — 이 값은 NODE_ENV=production 빌드(Docker 이미지 등)에서 데모 목적으로
# 노출하고 싶을 때만 명시 opt-in 하는 용도(codex review #39). (components/shell/dev-reset-button.tsx)
# ⚠️ 실서비스(prod)에서 true 로 두면 실수 클릭으로 세션이 즉시 초기화된다 — prod 배포는 반드시
#    false(또는 미설정) 유지.
NEXT_PUBLIC_ENABLE_DEV_RESET=false

# 기능 플래그 6종 — 값 계약(정본 lib/flags.ts): `1` 이면 노출, **그 외 값·미설정이면 차단**
# (`process.env.X === '1'`). 전부 "BE 미구현 표면의 mock 을 실제처럼 노출하지 않기" 위한 게이트라
# 이 템플릿의 기본값은 **전부 0(차단)** 으로 둔다 — 배포 파이프라인이 이 파일을 그대로 복사해
# 채워도 미구현 기능이 새지 않게(safe-by-default. `NEXT_PUBLIC_*` 는 next build 시점에 번들에
# 구워지므로 잘못 켠 값은 재빌드 전까지 못 되돌린다).
# ▶ 로컬에서 화면을 확인하려면 이 파일을 고치지 말고 `.env.local` 에서 해당 키만 1 로 올린다(opt-in).
# ▶ dev·prod 는 각 표면의 BE 가 준비된 뒤에만 배포 env(Vercel 프로젝트 env · Docker --build-arg)로 1.
# 게이트 사유·BE 준비 상태 상세는 lib/flags.ts 참고.

# 루틴 — 출시 시점 mock(미영속) 노출 차단. off 시 LNB·하단탭 항목 제외 + /planner/routine* redirect
# + 위저드 루틴 STEP 제외. 로컬 확인: .env.local 에 NEXT_PUBLIC_ROUTINE_ENABLED=1
NEXT_PUBLIC_ROUTINE_ENABLED=0
# 성장 리포트 — BE(회고·리포트 표면) 준비 전까지 mock 노출 차단. off 시 LNB·하단탭 제외 +
# /planner/reports redirect. 로컬 확인: .env.local 에 NEXT_PUBLIC_REPORTS_ENABLED=1
NEXT_PUBLIC_REPORTS_ENABLED=0
# 약점 자동 반영(풀림 분석) — BE 미구현(분석 표면·스케줄 엔진 소비 없음). off 시 위저드 STEP 6이
# mock 목록 대신 "출시 예정" 예고를 보여준다. 로컬 확인: .env.local 에 NEXT_PUBLIC_WEAKNESS_ENABLED=1
NEXT_PUBLIC_WEAKNESS_ENABLED=0
# 알림(리마인더·푸시) — BE 미구현(저장처 없음·웹푸시 인프라 전무). off 시 위저드 리마인더 STEP 제외.
# 로컬 확인: .env.local 에 NEXT_PUBLIC_NOTIFICATIONS_ENABLED=1
NEXT_PUBLIC_NOTIFICATIONS_ENABLED=0
# 풀림 Q 블록 딥링크(블록→풀이 직진입) — 플래너↔Q 연계가 붙기 전까지 off 유지. off 시 hasQAccess()가
# 항상 false. 로컬 확인: .env.local 에 NEXT_PUBLIC_Q_LINK_ENABLED=1
NEXT_PUBLIC_Q_LINK_ENABLED=0
# 홈 "오늘 회고" — BE 미구현(mock 계산만, 저장·소비 표면 없음). off 시 홈 일간 뷰 하단 리본 숨김.
# 로컬 확인: .env.local 에 NEXT_PUBLIC_REFLECTION_ENABLED=1
NEXT_PUBLIC_REFLECTION_ENABLED=0
